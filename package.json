{
  "name": "portfolio-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build && shx cp -r frontend/dist dist"
  },
  "devDependencies": {
    "shx": "^0.3.4"
  }
}
